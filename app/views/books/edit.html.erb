

<% if flash[:notice] %>
<p id = "notice">
  <%= flash[:notice] %>
</p>
<% end %>


<h1>Editing Book</h1>

<from class = "edut_book">
<%= form_with model: @book,url:book_path(@book.id),method: :patch do |f| %>

<% if @book.errors.any? %>
 <%= @book.errors.count %>errors prohibited this book from being saved:
 <ul>
   <% @book.errors.full_messages.each do |message| %>
   <li><%= message %></li>
   <% end %>
 </ul>
<% end %>

<div class=field>
  <label for = "book_title"><%= f.label :Title %><br/></label>
 <%= f.text_field :title %>
</div>

<div class = field>
  <label for = "book_body"><%= f.label :Body %><br/></label>
  <%= f.text_area :body %>
</div>

<div class = "actions">
<%= f.submit 'Update Book' %>
</div>

<% end %>

</from>

<%= link_to "show",book_path(@book.id) %>
|
<%= link_to "Back",books_path %>